{
  "name": "01pg",
  "version": "1.0.0",
  "description": "## 🐳 **Run PostgreSQL with Docker Locally**   Start a PostgreSQL container using Docker:   ```bash docker run --name my-postgres \\   -e POSTGRES_PASSWORD=mysecretpassword \\   -d -p 5432:5432 postgres ```",
  "main": "index.js",
  "scripts": {
    "build" : "tsc -b"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/pg": "^8.11.11",
    "pg": "^8.14.1"
  }
}
